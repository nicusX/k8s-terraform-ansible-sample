{
  "CN": "kubernetes",
  "hosts": [
    "worker0",
    "worker1",
    "worker2",
    "${kubernetes_cluster_dns}",
    "${controller0_ip}",
    "${controller1_ip}",
    "${controller2_ip}",
    "${etcd0_ip}",
    "${etcd1_ip}",
    "${etcd2_ip}",
    "${worker0_ip}",
    "${worker1_ip}",
    "${worker2_ip}",
    "${controller0_dns}",
    "${controller1_dns}",
    "${controller2_dns}",
    "${etcd0_dns}",
    "${etcd1_dns}",
    "${etcd2_dns}",
    "${worker0_dns}",
    "${worker1_dns}",
    "${worker2_dns}",
    "${kubernetes_api_elb_dns_name}",
    "127.0.0.1"
  ],
  "key": {
    "algo": "rsa",
    "size": 2048
  },
  "names": [
    {
      "C": "UK",
      "L": "London",
      "O": "OpenCredo",
      "OU": "Cluster"
    }
  ]
}
